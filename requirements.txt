# Legal Research & Paralegal MCP Server dependencies
# Install with: pip install -r requirements.txt

# --- Core (runtime) ---
# The official Model Context Protocol SDK, including FastMCP and the
# `mcp` CLI (which bundles the MCP Inspector integration).
mcp[cli]>=1.28.0

# --- Development & testing ---
pytest>=9.0
pytest-asyncio>=1.0
pytest-cov>=7.0
httpx>=0.28

# --- Linting & type checking ---
black>=26.0
flake8>=7.0
mypy>=2.0
